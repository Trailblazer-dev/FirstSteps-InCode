def fol():
    global x
    x = "fantastic"
fol()
print("python is"+x)
import matplotlib
print(matplotlib.__version__)
import matplotlib.pyplot as plt
import numpy as np
keys = np.array([1,8])
locks = np.array([3,10 ])
plt.plot(keys, locks)# Here is parameter you can choose to  plot without a line by adding a 3rd paramete in plot(1st,2nd,'o')
plt.show()    #These is amazing it gives output as a graph
